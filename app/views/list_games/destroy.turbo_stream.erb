<%= turbo_stream.replace list_panel_item_id(@list, params[:scope]) do %>
  <%= render "games/list_panel_item", list: @list, existing: nil, game: @game, scope: params[:scope], origin: params[:origin] %>
<% end %>
